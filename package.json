{
  "name": "golsz",
  "private": true,
  "type": "module",
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "scripts": {
    "test": "node tests/run-all.cjs",
    "check": "for f in api/*.js; do node --check \"$f\" || exit 1; done && node tests/run-all.cjs",
    "deploy": "npm run check && npx vercel --prod --yes"
  },
  "devDependencies": {
    "@babel/parser": "^7.29.8",
    "@babel/traverse": "^8.0.4"
  }
}
